import { ShortWordPipe } from './short-word.pipe';

describe('ShortWordPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortWordPipe();
    expect(pipe).toBeTruthy();
  });
});
